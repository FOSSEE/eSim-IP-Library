/* This is cfunc.mod file auto generated by gen_con_info.py
        Developed by Sumanto Kar at IIT Bombay */

                
        #include <stdio.h>
        #include <math.h>
        #include <string.h>
        #include "sim_main_wallacemul66.h"

        
void cm_wallacemul66(ARGS) 
{
	Digital_State_t *_op_prod, *_op_prod_old;

    static int inst_count=0;
    int count=0;
        
    if(INIT)
    {
        inst_count++;
        PARAM(instance_id)=inst_count;
        foowallacemul66(0,inst_count);
        /* Allocate storage for output ports and set the load for input ports */

        
        port_A=PORT_SIZE(A);

        port_B=PORT_SIZE(B);

        port_prod=PORT_SIZE(prod);
		cm_event_alloc(0,6*sizeof(Digital_State_t));
		/* set the load for input ports. */
		int Ii;
		for(Ii=0;Ii<PORT_SIZE(A);Ii++)
		{
			LOAD(A[Ii])=PARAM(input_load); 
		}
		for(Ii=0;Ii<PORT_SIZE(B);Ii++)
		{
			LOAD(B[Ii])=PARAM(input_load); 
		}

		/*Retrieve Storage for output*/
		_op_prod = _op_prod_old = (Digital_State_t *) cm_event_get_ptr(0,0);


	}
	else
	{
		_op_prod = (Digital_State_t *) cm_event_get_ptr(0,0);
		_op_prod_old = (Digital_State_t *) cm_event_get_ptr(0,1);
	}

	//Formating data for sending it to client
	int Ii;
	count=(int)PARAM(instance_id);

    for(Ii=0;Ii<PORT_SIZE(A);Ii++)
    {
        if( INPUT_STATE(A[Ii])==ZERO )
        {
            temp_A[Ii]=0;            }
        else
        {
            temp_A[Ii]=1;
        }
            }
    for(Ii=0;Ii<PORT_SIZE(B);Ii++)
    {
        if( INPUT_STATE(B[Ii])==ZERO )
        {
            temp_B[Ii]=0;            }
        else
        {
            temp_B[Ii]=1;
        }
            }
	foowallacemul66(1,count);

	/* Scheduling event and processing them */
    for(Ii=0;Ii<PORT_SIZE(prod);Ii++)
    {
        if(temp_prod[Ii]==0)
        {
            _op_prod[Ii]=ZERO;
            }
        else if(temp_prod[Ii]==1)
        {
            _op_prod[Ii]=ONE;
            }
        else
        {
            printf("Unknown value\n");
                }

        if(ANALYSIS == DC)
        {
            OUTPUT_STATE(prod[Ii]) = _op_prod[Ii];
            }
        else if(_op_prod[Ii] != _op_prod_old[Ii])
        {
            OUTPUT_STATE(prod[Ii]) = _op_prod[Ii];
            OUTPUT_DELAY(prod[Ii]) = ((_op_prod[Ii] == ZERO) ? PARAM(fall_delay) : PARAM(rise_delay));
            }
        else
        {
            OUTPUT_CHANGED(prod[Ii]) = FALSE;
            }
        OUTPUT_STRENGTH(prod[Ii]) = STRONG;
    }

}